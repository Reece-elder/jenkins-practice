pipeline {
    agent any
    stages {
        stage('Pipeline Stages'){
            steps {
                sh "ls"
                sh "pwd"
                sh "mkdir pipeline_demo"
                sh "cd pipeline_demo"
                sh "pwd"
            }
        }
    }
}